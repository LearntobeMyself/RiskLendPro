# 金融系统接口文档

**基础路径**: `http://47.109.109.231:8080/api/v1`

***

## 逾期等级与额度调整规则

### 1. 逾期等级定义

| 逾期等级 | 逾期天数   | 说明   |
| ---- | ------ | ---- |
| N    | 无逾期    | 正常状态 |
| M1   | 1-30天  | 轻度逾期 |
| M2   | 31-60天 | 中度逾期 |
| M3   | 61-90天 | 重度逾期 |
| M4   | 90天以上  | 严重逾期 |

### 2. 自动额度调整规则

系统每天凌晨2点会自动检查所有逾期用户，并根据逾期等级调整其信用额度：

| 逾期等级 | 调整后额度比例  | 调整原因      |
| ---- | -------- | --------- |
| M1   | 原额度的 80% | 轻度逾期，风险可控 |
| M2   | 原额度的 50% | 中度逾期，风险较高 |
| M3   | 原额度的 20% | 重度逾期，风险严重 |
| M4   | 额度清零     | 严重逾期，风险极高 |

### 3. 额度调整流程

1. **系统自动调整**：每日凌晨2点自动执行
2. **管理员手动调整**：管理员可通过 `/admin/limit/adjust` 接口手动调整用户额度
3. **调整记录**：所有额度调整都会记录到 `limit_adjust_log` 表中，包含调整前后的额度、调整原因和操作人

### 4. 逾期处理机制

- **还款提醒**：系统会在还款到期前3天、到期当天发送邮件提醒
- **逾期通知**：逾期第一天发送邮件通知用户
- **状态更新**：每日凌晨自动更新逾期状态和等级
- **额度影响**：逾期会直接影响用户的信用额度和借款资格

***

## JWT认证说明

- **认证方式**：本系统使用JWT (JSON Web Token)进行接口认证
- **认证范围**：除以下接口外，所有其他接口都需要在请求头中携带JWT令牌
- **不需要认证的接口**：
  - `/auth/register` - 用户注册
  - `/auth/login` - 用户登录
  - `/admin/register` - 管理员注册
  - `/admin/login` - 管理员登录
- **认证头部**：`Authorization: Bearer {token}`

***

## 统一响应格式

所有接口的响应都遵循以下统一格式：

```json
{
  "code": 200,
  "message": "操作成功",
  "success": true,
  "data": {}
}
```

| 字段      | 类型      | 说明                       |
| ------- | ------- | ------------------------ |
| code    | int     | 状态码，200表示成功，其他表示失败       |
| message | String  | 响应消息                     |
| success | boolean | 是否成功                     |
| data    | Object  | 响应数据，成功时返回业务数据，失败时返回null |

***

## 业务状态码说明

| 状态码            | 说明    |
| -------------- | ----- |
| WAITING        | 待处理   |
| SYSTEM\_REJECT | 系统拒绝  |
| MANUAL\_REVIEW | 人工复核中 |
| FINAL\_PASS    | 已通过   |
| FINAL\_REJECT  | 已拒绝   |

***

## 一、认证模块 (Authentication)

**核心逻辑**: 统一注册/登录。登录后返回 token 和 role。如果是用户，返回其当前的评估状态（未评估/评估中/已获额度）。

### 1.1 用户注册

- **URL**: `/auth/register`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 用户注册接口

**请求参数**:

| 参数名         | 类型     | 必填 | 说明       |
| ----------- | ------ | -- | -------- |
| realName    | String | 是  | 用户姓名     |
| phoneNumber | String | 是  | 用户手机号    |
| email       | String | 是  | 用户邮箱     |
| idCard      | String | 是  | 用户身份证号   |
| password    | String | 是  | 用户密码     |
| repassword  | String | 是  | 用户重新输入密码 |

**响应示例**:

```json
{
  "code": 200,
  "message": "注册成功",
  "success": true,
  "data": null
}
```

### 1.2 用户登录

- **URL**: `/auth/login`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 用户登录接口，登录后返回token和role，如果是用户还返回当前评估状态

**请求参数**:

| 参数名         | 类型     | 必填 | 说明    |
| ----------- | ------ | -- | ----- |
| phoneNumber | String | 是  | 用户手机号 |
| password    | String | 是  | 用户密码  |

**响应示例**:

```json
{
  "code": 200,
  "message": "登录成功",
  "success": true,
  "data": {
    "id": 1,
    "token": "eyJhbGciOiJIUzI1NiJ9...",
    "role": "USER",
    "assessmentStatus": "NOT_ASSESSED"  // NOT_ASSESSED=未评估, ASSESSING=评估中, APPROVED=已获额度
  }
}
```

***

## 二、授信评估模块 (Credit Assessment)

**核心逻辑**: 用户提交风控资料 -> 后端结合模拟数据发送给 Python -> Python 算出分数和初始额度。

**业务说明**:

- Android端填写个人信息
- 后端和模拟外部数据查询（黑名单？是否多平台贷款过？）
- Python风控决策（80分以上信用良好，按照收入和信用分配借款额度）（60-80分人工审批）（60分以下，信用不好不可以借钱）
- 人工审批（管理员登录 Web 后台，查看那些状态为 MANUAL\_REVIEW（人工复核）的订单。管理员点击"拒绝"或"通过"，并给出最终的贷款额度）
- 邮箱通知，安卓端看见评估状态从"评估中"变成了"已通过"，并显示出额度（比如 20000.00）。

### 2.1 提交风控评估申请

- **URL**: `/risk/assessment/submit`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 用户点击提交申请，启动风控评估流程

**请求参数**:

| 参数名           | 类型      | 必填 | 说明              |
| ------------- | ------- | -- | --------------- |
| idCard        | String  | 是  | 身份证号（唯一标识）      |
| name          | String  | 是  | 用户姓名            |
| phone         | String  | 是  | 手机号码            |
| email         | String  | 是  | 接收结果的邮箱         |
| gender        | Integer | 是  | 0-女, 1-男        |
| birthday      | String  | 是  | 出生日期 YYYY-MM-DD |
| education     | String  | 是  | 见附录数据字典         |
| marriage      | String  | 是  | 见附录数据字典         |
| jobType       | String  | 是  | 见附录数据字典         |
| monthlyIncome | String  | 是  | 见附录数据字典         |
| hasHouse      | Boolean | 是  | 是否有房            |
| hasCar        | Boolean | 是  | 是否有车            |
| contactPhone  | String  | 是  | 紧急联系人电话         |

**响应示例**:

```json
{
  "code": 200,
  "message": "申请已受理，风控评估启动",
  "success": true,
  "data": {
    "applyId": "L20231027001",
    "submitTime": "2023-10-27 14:00:00",
    "status": "WAITING"
  }
}
```

### 2.2 查询评估状态

- **URL**: `/risk/assessment/status`
- **方式**: GET
- **描述**: Android端轮询查询评估状态

**请求参数**:

| 参数名     | 类型     | 必填 | 说明   |
| ------- | ------ | -- | ---- |
| applyId | String | 是  | 申请ID |

**响应示例**:

```json
{
  "code": 200,
  "message": "风控信息查询",
  "success": true,
  "data": {
    "applyId": "L20231027001",
    "status": "MANUAL_REVIEW",
    "statusTitle": "人工复核中",
    "isFinal": false
  }
}
```

### 2.3 获取最终额度结果

- **URL**: `/risk/assessment/result`
- **方式**: GET
- **描述**: 获取最终评估额度（如20,000）和失效日期

**请求参数**:

| 参数名     | 类型     | 必填 | 说明   |
| ------- | ------ | -- | ---- |
| applyId | String | 是  | 申请ID |

**响应示例**:

```json
{
  "code": 200,
  "message": "获取评估结果成功",
  "success": true,
  "data": {
    "applyId": "L20231027001",
    "totalScore": 76,
    "creditLimit": 20000.00,
    "expireDate": "2024-12-31",
    "status": "FINAL_PASS",
    "approvalTime": "2023-10-27 15:30:00"
  }
}
```

***

## 三、借款/支用模块 (Withdrawal/Loan)

**核心逻辑**:

- 用户发起借款时，系统首先检查借款金额与用户剩余额度的关系
- **额度内借款（金额 ≤ 剩余额度）**：系统自动审批通过，直接发放借款并扣减额度，无需管理员操作
- **额度外借款（金额 > 剩余额度）**：系统生成"贷款申请"记录，需要管理员审批后发放借款，审批通过后邮件通知用户

**业务状态码说明（借款模块）**:

| 借款状态码             | 说明               |
| ----------------- | ---------------- |
| PENDING\_APPROVAL | 待审批（额度外借款需管理员审批） |
| APPROVED          | 已审批通过            |
| REJECTED          | 审批拒绝             |
| DISBURRSED        | 已发放              |
| REPAID            | 已还清              |
| OVERDUE           | 逾期中              |

### 3.1 发起借款请求

- **URL**: `/loan/request`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 用户输入"借款金额"和"还款期限"，系统判断借款金额与用户剩余额度的关系

**业务逻辑**:

```
用户发起借款
    ↓
检查用户是否有未处理逾期
    ↓
检查借款金额 vs 剩余额度
    ↓
├── 金额 ≤ 额度 → 自动审批通过 → 状态变为 DISBURRSED → 扣减额度 → 直接放款
└── 金额 > 额度 → 状态变为 PENDING_APPROVAL → 等待管理员审批
```

**请求参数**:

| 参数名             | 类型         | 必填 | 说明                   |
| --------------- | ---------- | -- | -------------------- |
| amount          | BigDecimal | 是  | 借款金额                 |
| termMonths      | Integer    | 是  | 还款期限（月）              |
| repaymentMethod | String     | 是  | 还款方式（等额本息/等额本金/先息后本） |

**响应示例（额度内借款 - 自动通过）**:

```json
{
  "code": 200,
  "message": "借款发放成功",
  "success": true,
  "data": {
    "loanId": 1,
    "userId": 1001,
    "amount": 5000.00,
    "termMonths": 12,
    "interestRate": 0.05,
    "repaymentMethod": "等额本息",
    "status": "DISBURRSED",
    "disbursementTime": "2023-10-28 10:00:00",
    "autoApproved": true,
    "remark": "额度充足，自动审批通过"
  }
}
```

**响应示例（额度外借款 - 需要审批）**:

```json
{
  "code": 200,
  "message": "借款申请已提交，等待管理员审批",
  "success": true,
  "data": {
    "loanId": 2,
    "userId": 1001,
    "amount": 25000.00,
    "termMonths": 12,
    "interestRate": 0.05,
    "repaymentMethod": "等额本息",
    "status": "PENDING_APPROVAL",
    "applyTime": "2023-10-28 10:00:00",
    "autoApproved": false,
    "remark": "借款金额超出剩余额度(15000.00)，请等待管理员审批"
  }
}
```

**响应示例（借款失败 - 有未处理逾期）**:

```json
{
  "code": 400,
  "message": "存在未处理逾期记录，无法发起新借款",
  "success": false,
  "data": null
}
```

**响应示例（借款失败 - 未通过风控评估）**:

```json
{
  "code": 400,
  "message": "未通过风控评估，无法发起借款",
  "success": false,
  "data": {
    "reason": "NO_CREDIT_LIMIT",
    "detail": "您尚未完成风控评估或评估未通过，请先完成授信评估"
  }
}
```

### 3.2 获取用户借款记录

- **URL**: `/loan/user/history`
- **方式**: GET
- **描述**: 获取用户的历史借款记录

**请求参数**:

| 参数名      | 类型      | 必填 | 说明        |
| -------- | ------- | -- | --------- |
| pageNum  | Integer | 否  | 页码，默认1    |
| pageSize | Integer | 否  | 每页条数，默认10 |

**响应示例**:

```json
{
  "code": 200,
  "message": "获取借款记录成功",
  "success": true,
  "data": {
    "records": [
      {
        "loanId": 1,
        "amount": 20000.00,
        "termMonths": 12,
        "interestRate": 0.05,
        "status": "DISBURRSED",
        "disbursementTime": "2023-10-28 10:00:00"
      }
    ],
    "total": 5,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

### 3.3 获取用户贷款申请记录

- **URL**: `/loan/user/applications`
- **方式**: GET
- **描述**: 获取用户提交的贷款申请记录，包含待审批和已审批结果

**请求参数**:

| 参数名      | 类型      | 必填 | 说明                                        |
| -------- | ------- | -- | ----------------------------------------- |
| status   | String  | 否  | 筛选状态（PENDING\_APPROVAL/APPROVED/REJECTED） |
| pageNum  | Integer | 否  | 页码，默认1                                    |
| pageSize | Integer | 否  | 每页条数，默认10                                 |

**响应示例**:

```json
{
  "code": 200,
  "message": "获取贷款申请记录成功",
  "success": true,
  "data": {
    "records": [
      {
        "loanId": 2,
        "userId": 1001,
        "amount": 25000.00,
        "termMonths": 12,
        "interestRate": 0.05,
        "repaymentMethod": "等额本息",
        "status": "PENDING_APPROVAL",
        "applyTime": "2023-10-28 10:00:00",
        "approvedTime": null,
        "rejectReason": null
      },
      {
        "loanId": 3,
        "userId": 1001,
        "amount": 20000.00,
        "termMonths": 6,
        "interestRate": 0.05,
        "repaymentMethod": "等额本息",
        "status": "APPROVED",
        "applyTime": "2023-10-20 09:00:00",
        "approvedTime": "2023-10-20 14:30:00",
        "rejectReason": null
      }
    ],
    "total": 2,
    "size": 10,
    "current": 1,
    "pages": 1
  }
}
```

***

## 四、账务与还款模块 (Repayment & Accounting)

**核心逻辑**: 借款成功后生成还款计划表。用户查看每期还多少钱、是否逾期。每日凌晨跑批更新逾期状态（从 N 变 M1，M1 变 M2）。

### 4.1 查看还款计划

- **URL**: `/repayment/plans`
- **方式**: GET
- **描述**: 用户查看自己的还款计划表，显示每期还款金额、是否逾期

**请求参数**: 无（用户ID从token中获取）

**响应示例**:

```json
{
  "code": 200,
  "message": "获取还款计划成功",
  "success": true,
  "data": [
    {
      "planId": 1,
      "loanId": 1,
      "totalAmount": 22000.00,
      "paidAmount": 0.00,
      "remainingAmount": 22000.00,
      "totalPeriods": 12,
      "currentPeriod": 0,
      "status": "ACTIVE",
      "createTime": "2023-10-28 10:00:00"
    }
  ]
}
```

### 4.2 执行还款

- **URL**: `/repayment/execute`
- **方式**: POST
- **描述**: 用户点击"还款"，执行单期还款操作

**请求参数**:

| 参数名    | 类型         | 必填 | 说明     |
| ------ | ---------- | -- | ------ |
| planId | Long       | 是  | 还款计划ID |
| period | Integer    | 是  | 期数     |
| amount | BigDecimal | 是  | 还款金额   |

**响应示例**:

```json
{
  "code": 200,
  "message": "还款成功",
  "success": true,
  "data": {
    "recordId": 1,
    "planId": 1,
    "period": 1,
    "actualAmount": 1833.33,
    "repaymentDate": "2023-11-28 10:00:00",
    "status": "COMPLETED"
  }
}
```

### 4.3 获取还款记录详情

- **URL**: `/repayment/record/{planId}`
- **方式**: GET
- **描述**: 获取指定还款计划的全部还款记录

**请求参数**:

| 参数名    | 类型     | 必填 | 说明     |
| ------ | ------ | -- | ------ |
| planId | Long   | 是  | 还款计划ID |
| status | String | 否  | 按状态筛选  |

**响应示例**:

```json
{
  "code": 200,
  "message": "获取还款记录成功",
  "success": true,
  "data": [
    {
      "recordId": 1,
      "planId": 1,
      "period": 1,
      "principal": 1600.00,
      "interest": 233.33,
      "amount": 1833.33,
      "actualAmount": 1833.33,
      "dueDate": "2023-11-28",
      "repaymentDate": "2023-11-28 10:00:00",
      "status": "COMPLETED"
    }
  ]
}
```

### 4.5 获取还款统计

- **URL**: `/repayment/statistics`
- **方式**: GET
- **描述**: 获取所有用户的还款成功统计（总数和已还款数）

**响应示例**:

```json
{
  "code": 200,
  "message": "获取还款统计成功",
  "success": true,
  "data": {
    "totalCount": 1000,
    "completedCount": 850
  }
}
```

### 4.6 获取逾期统计

- **URL**: `/repayment/overdue`
- **方式**: GET
- **描述**: 获取所有还款计划的逾期统计（总数和逾期数）

**响应示例**:

```json
{
  "code": 200,
  "message": "获取逾期统计成功",
  "success": true,
  "data": {
    "totalCount": 500,
    "overdueCount": 50
  }
}
```

***

## 五、额度管理模块 (Post-loan & Limit Management)

**核心逻辑**: 用户查看当前总额度、已用额度、剩余额度。

### 5.1 查看用户额度

- **URL**: `/user/credit-limit`
- **方式**: GET
- **描述**: 用户查看自己的当前总额度、已用额度、剩余额度

**请求参数**: 无（用户ID从token中获取）

**响应示例**:

```json
{
  "code": 200,
  "message": "获取额度信息成功",
  "success": true,
  "data": {
    "userId": 1001,
    "totalLimit": 20000.00,
    "usedLimit": 5000.00,
    "remainingLimit": 15000.00,
    "overdueAmount": 0.00,
    "hasOverdue": false,
    "lastUpdateTime": "2023-10-28 10:00:00"
  }
}
```

***

## 六、管理员审批与看板模块 (Admin Dashboard)

**核心逻辑**: 审批 MANUAL\_REVIEW 的订单，并查看整体风险情况。支持Vintage曲线和滚动率等BI分析。

### 6.1 获取待审批列表

- **URL**: `/admin/risk/list`
- **方式**: GET
- **描述**: 获取所有状态为MANUAL\_REVIEW的订单列表

**请求参数**:

| 参数名    | 类型      | 必填 | 说明        |
| ------ | ------- | -- | --------- |
| page   | Integer | 否  | 页码，默认1    |
| size   | Integer | 否  | 每页条数，默认10 |
| status | String  | 否  | 筛选状态      |

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "success": true,
  "data": {
    "total": 125,
    "current_page": 1,
    "pages": 13,
    "list": [
      {
        "applyId": "L20231027001",
        "userName": "张三",
        "phone": "138****8000",
        "idCard": "510***********1234",
        "totalScore": 76,
        "sysDecision": "REVIEW",
        "status": "MANUAL_REVIEW",
        "applyTime": "2023-10-27 14:00:00"
      }
    ]
  }
}
```

### 6.2 获取详细风控报告（修改版）

- **URL**: `/admin/risk/report/{applyId}`
- **方式**: GET
- **描述**: 获取Java决策引擎生成的详细JSON报告，包含得分拆解和外部特征数据（从数据库查询）

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "success": true,
  "data": {
    "userDetails": {
      "name": "张*",
      "idCard": "510***********1234",
      "education": "本科",
      "marriage": "已婚",
      "jobType": "企事业单位",
      "monthlyIncome": "15000以上",
      "hasHouse": true,
      "hasCar": false,
      "age": 28
    },
    "totalScore": 72.0,
    "systemDecision": "MANUAL_REVIEW",
    "suggestedAmount": 30000,
    "scoreDetails": [
      {"feature": "age_26_35", "value": 1.0, "weight": 0.05, "contribution": 5.0, "description": "年龄处于稳定期"},
      {"feature": "income_15000_plus", "value": 1.0, "weight": 0.25, "contribution": 25.0, "description": "收入较高"},
      {"feature": "multi_head_4_6", "value": 1.0, "weight": -0.25, "contribution": -25.0, "description": "多头借贷5家，风险偏高"},
      {"feature": "credit_query_4_8", "value": 1.0, "weight": -0.35, "contribution": -35.0, "description": "近3月征信查询6次，异常频繁"},
      {"feature": "overdue_12m_0", "value": 1.0, "weight": 0.1, "contribution": 10.0, "description": "近12月无逾期"}
    ],
    "externalFeatures": {
      "creditScore": 680,
      "overdueCount12m": 0,
      "creditQueryCount3m": 6,
      "multiHeadLoanCount": 5,
      "multiHeadLoanTotalAmount": 80000,
      "deviceIsVirtual": 0,
      "ipIsProxy": 0,
      "dataSource": "EXTERNAL_SYNC",
      "updatedAt": "2023-10-26 02:00:00"
    },
    "blacklistCheck": {
      "hit": false,
      "source": null,
      "reason": null
    }
  }
}
```

### 6.3 管理员最终审批决策

- **URL**: `/admin/risk/approve`
- **方式**: POST
- **描述**: 管理员给出最终贷款额度，审批结果邮件通知用户

**请求参数**:

| 参数名         | 类型         | 必填 | 说明                    |
| ----------- | ---------- | -- | --------------------- |
| applyId     | String     | 是  | 申请ID                  |
| auditResult | String     | 是  | PASS(通过) 或 REJECT(拒绝) |
| creditLimit | BigDecimal | 否  | 最终额度（通过时必填）           |
| auditRemark | String     | 是  | 审批评语                  |

**响应示例**:

```json
{
  "code": 200,
  "message": "审批成功，结果已邮件通知用户",
  "success": true,
  "data": null
}
```

### 6.4 获取Vintage曲线数据

- **URL**: `/admin/bi/vintage`
- **方式**: GET
- **描述**: 获取Vintage曲线数据，用于分析不同放款月份的逾期率趋势

**响应示例**:

```json
{
  "code": 200,
  "message": "获取Vintage数据成功",
  "success": true,
  "data": {
    "months": ["2023-07", "2023-08", "2023-09", "2023-10"],
    "vintageData": [
      {"month": "2023-07", "disbursedAmount": 500000, "M1Rate": 0.02, "M2Rate": 0.01, "M3Rate": 0.005},
      {"month": "2023-08", "disbursedAmount": 600000, "M1Rate": 0.015, "M2Rate": 0.008, "M3Rate": 0.003}
    ]
  }
}
```

### 6.5 获取滚动率数据

- **URL**: `/admin/bi/roll-rate`
- **方式**: GET
- **描述**: 获取滚动率数据，用于分析逾期账户在各状态间的转化情况

**响应示例**:

```json
{
  "code": 200,
  "message": "获取滚动率数据成功",
  "success": true,
  "data": {
    "currentStatus": "C",
    "nextMonthStatus": {
      "C": 0.85,
      "M1": 0.10,
      "M2": 0.03,
      "M3": 0.02
    }
  }
}
```

### 6.6 获取管理员首页统计

- **URL**: `/admin/dashboard/stats`
- **方式**: GET
- **描述**: 获取管理员首页的汇总统计数据

**响应示例**:

```json
{
  "code": 200,
  "message": "获取统计数据成功",
  "success": true,
  "data": {
    "totalApplications": 1250,
    "pendingReview": 35,
    "approvedToday": 12,
    "rejectedToday": 3,
    "totalDisbursed": 5000000.00,
    "totalOverdue": 150000.00,
    "overdueRate": 0.03
  }
}
```

### 6.7 获取待审批贷款申请列表

- **URL**: `/admin/loan/pending-list`
- **方式**: GET
- **描述**: 获取所有状态为PENDING\_APPROVAL的贷款申请列表，管理员可进行审批操作

**请求参数**:

| 参数名  | 类型      | 必填 | 说明        |
| ---- | ------- | -- | --------- |
| page | Integer | 否  | 页码，默认1    |
| size | Integer | 否  | 每页条数，默认10 |

**响应示例**:

```json
{
  "code": 200,
  "message": "查询成功",
  "success": true,
  "data": {
    "total": 15,
    "current_page": 1,
    "pages": 2,
    "list": [
      {
        "loanId": 2,
        "userId": 1001,
        "userName": "张三",
        "phone": "138****8000",
        "idCard": "510***********1234",
        "amount": 25000.00,
        "termMonths": 12,
        "repaymentMethod": "等额本息",
        "currentLimit": 15000.00,
        "exceedAmount": 10000.00,
        "applyTime": "2023-10-28 10:00:00",
        "status": "PENDING_APPROVAL"
      }
    ]
  }
}
```

### 6.8 管理员审批贷款申请

- **URL**: `/admin/loan/approve`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 管理员审批用户提交的额度外贷款申请，审批通过后邮件通知用户

**业务逻辑**:

```
管理员审批贷款申请
    ↓
├── 审批拒绝 → 状态变为 REJECTED → 邮件通知用户
└── 审批通过 → 状态变为 APPROVED → 计算实际发放金额(原额度+新审批额度) → 邮件通知用户
```

**请求参数**:

| 参数名             | 类型         | 必填 | 说明                          |
| --------------- | ---------- | -- | --------------------------- |
| loanId          | Long       | 是  | 贷款申请ID                      |
| approveResult   | String     | 是  | APPROVE(通过) 或 REJECT(拒绝)    |
| additionalLimit | BigDecimal | 否  | 额外批准的额度（通过时必填，用于补偿超出原额度的部分） |
| approveRemark   | String     | 是  | 审批评语                        |

**响应示例（审批通过）**:

```json
{
  "code": 200,
  "message": "审批成功，已邮件通知用户",
  "success": true,
  "data": {
    "loanId": 2,
    "userId": 1001,
    "originalLimit": 15000.00,
    "additionalLimit": 10000.00,
    "totalLimit": 25000.00,
    "actualDisbursedAmount": 25000.00,
    "status": "APPROVED",
    "approveTime": "2023-10-28 15:00:00",
    "emailSent": true
  }
}
```

**响应示例（审批拒绝）**:

```json
{
  "code": 200,
  "message": "审批已拒绝，已邮件通知用户",
  "success": true,
  "data": {
    "loanId": 2,
    "userId": 1001,
    "status": "REJECTED",
    "approveTime": "2023-10-28 15:00:00",
    "rejectReason": "借款金额过高，风险控制不符合要求",
    "emailSent": true
  }
}
```

***

## 八、管理员认证模块 (Admin Auth)

**核心逻辑**: 管理员注册和登录，与用户认证分离。

### 8.1 管理员注册

- **URL**: `/admin/register`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 管理员注册接口

**请求参数**:

| 参数名         | 类型     | 必填 | 说明   |
| ----------- | ------ | -- | ---- |
| username    | String | 是  | 昵称   |
| password    | String | 是  | 密码   |
| rePassword  | String | 是  | 二次密码 |
| phoneNumber | String | 是  | 手机号  |
| email       | String | 是  | 用户邮箱 |

**响应示例**:

```json
{
  "code": 200,
  "message": "注册成功",
  "success": true,
  "data": null
}
```

### 8.2 管理员登录

- **URL**: `/admin/login`
- **方式**: POST
- **Content-Type**: application/json
- **描述**: 管理员登录接口

**请求参数**:

| 参数名      | 类型     | 必填 | 说明    |
| -------- | ------ | -- | ----- |
| username | String | 是  | 管理员昵称 |
| password | String | 是  | 管理员密码 |

**响应示例**:

```json
{
  "code": 200,
  "message": "登录成功",
  "success": true,
  "data": {
    "id": 1,
    "token": "eyJhbGciOiJIUzI1NiJ9..."
  }
}
```

***

## 附录：风控全量数据字典

用户在这些维度里，只能输入以下枚举值，前端与后端必须严格按此传值。

| 维度                       | 枚举值                                                                                                       |
| ------------------------ | --------------------------------------------------------------------------------------------------------- |
| **education**            | "博士", "硕士", "本科", "大专", "高中及以下"                                                                           |
| **marriage**             | "单身", "已婚", "离异", "丧偶"                                                                                    |
| **monthly\_income**      | "3000以下", "3000-8000", "8000-15000", "15000以上"                                                            |
| **job\_type**            | "企事业单位", "私营企业", "外资/合资", "个体经营", "自由职业", "学生"                                                            |
| **status (业务状态)**        | "WAITING"(待处理), "SYSTEM\_REJECT"(系统拒绝), "MANUAL\_REVIEW"(人工复核中), "FINAL\_PASS"(已通过), "FINAL\_REJECT"(已拒绝) |
| **sys\_decision (引擎建议)** | "APPROVE"(建议通过), "REVIEW"(建议人工复核), "REJECT"(建议拒绝)                                                         |

