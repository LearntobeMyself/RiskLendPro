1. 完整流程总结
   ### Python端（离线训练）
   ```
   train_scoring_model.py
       ↓
   产出 scoring_rules.json
   {
     "version": "v2.3",
     "feature_weights": {
       "age": -0.186,
       "income": -7.67e-05,
       "multi_head_loan_count": -0.104,
       ...
     },
     "intercept": -0.0307,
     "thresholds": {
       "auto_approve": 80,
       "manual_review": 60
     }
   }
       ↓
   load_to_mysql.py
       ↓
   写入 credit_data_db.scoring_rules 表
   ```
   ### Java端（在线评分）
   ```
   用户提交申请
       ↓
   1. 查询黑名单（blacklist表）
       ↓
   2. 查询用户外部特征
   （user_external_features表）
       ↓
   3. 获取评分规则（scoring_rules表）
       ↓
   4. 使用Python训练的特征权重计算评分：
      score = intercept
      score += age_weight × age
      score += income_weight × income
      score += multi_head_weight × 
      multi_head_count
      ...
       ↓
   5. Sigmoid转换为0-100分
       ↓
   6. 决策输出
   ```
   ### 特征权重（Python训练产出）
   特征 权重 说明 age -0.186 年龄原始权重 income -7.67e-05 收入原始权重 multi\_head\_loan\_count -0.104 多头借贷数量权重 overdue\_count\_12m 6.365 逾期次数权重 overdue\_bin\_overdue\_12m\_3\_plus 2.12 逾期3次以上分箱权重

### 额度计算流程（新版）

```
基础额度 = 月收入 × 倍数（根据评分档位）
    ↓
根据外部特征数据扣减：
├── 多头借贷 > 3家平台 → 每超1家扣10%
├── 逾期记录 → 每1次扣15%
├── 征信查询 > 5次 → 扣5%
├── 虚拟设备 → 扣50%
├── 代理IP → 扣20%
└── 多头借贷金额 > 年收入50% → 扣10%
    ↓
最终额度 = 基础额度 × (1 - 扣减比例)
```

### 完整风控流程

```
用户提交申请
    ↓
1. 查询黑名单表（credit_data_db.
blacklist）
    ↓ 命中 → SYSTEM_REJECT
2. 查询用户外部特征表（credit_data_db.
user_external_features）
    ↓
3. 使用Python训练的特征权重计算评分
    ↓
4. 决策：
    - >= 80分 → APPROVE → 计算额度（参
    考外部特征扣减）
    - 60-79分 → REVIEW
    - < 60分 → REJECT
    ↓
5. 创建 UserCreditLimit 记录
```

现在评分和额度计算都完全基于Python分析产出的数据了！

<br />

<br />

1. <br />

   这些接口只要有了token全部都可以访问
2. 反复申请这个评估，这个有问题
3. 还有管理员查看这个详细的风控信息，当用户被系统拒绝时，或者直接通过时不应该把风控详细信息记录到redis中，这个查看详细的风控信息应该只留人工审核

<br />

```shellscript
cd d:\javacode\RiskLendPro
mvn spring-boot:run
```

