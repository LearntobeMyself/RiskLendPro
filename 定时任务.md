# 扣子黑名单同步定时任务（Java 触发）

> 因扣子平台无法使用 Cron，**定时触发由 Java 后端承担**；扣子工作流仍负责爬取温州 CSV、清洗、循环调用 `/sync/blacklist`。

## 架构

```
CozeBlacklistSyncJob（@Scheduled，默认每日 02:00）
  → CozeWorkflowService.run()
  → POST https://api.coze.cn/v1/workflow/run
  → 扣子工作流：抓取 → 清洗 → POST {SPRING_BASE_URL}/api/v1/sync/blacklist
  → credit_data_db.blacklist 增量入库
```

实现类：

| 类 | 路径 |
|----|------|
| 配置 | `src/main/java/.../config/CozeWorkflowProperties.java` |
| HTTP | `src/main/java/.../config/RestTemplateConfig.java` |
| 调用 | `src/main/java/.../service/coze/CozeWorkflowService.java` |
| 定时 | `src/main/java/.../job/CozeBlacklistSyncJob.java` |

`RiskLendProApplication` 已启用 `@EnableScheduling`，无需额外改动。

---

## 配置项（application.yaml）

```yaml
risk:
  coze:
    enabled: false                              # 生产改为 true
    api-url: https://api.coze.cn/v1/workflow/run
    token: ${COZE_API_TOKEN:}                   # PAT，勿提交仓库
    workflow-id: "7647934026113089570"
    workflow-version: "v0.0.3"
    cron: "0 0 2 * * ?"                         # 每日 02:00
    read-timeout-ms: 300000                     # 爬取+批量 POST 可能较久
    connect-timeout-ms: 10000
```

| 项 | 说明 |
|----|------|
| `enabled` | `false` 时不注册 Job、不调用扣子 |
| `token` | 扣子 PAT，建议环境变量 `COZE_API_TOKEN` 注入 |
| `cron` | 本地测试可临时改为 `0/30 * * * * ?`（每 30 秒），**测完改回** |

---

## Docker 启动示例

```bash
mkdir -p /root/springboot/upload

docker run -d \
  --name risk-lend-pro \
  --restart=always \
  -p 8080:8080 \
  -e TZ=Asia/Shanghai \
  -e COZE_API_TOKEN=pat_你的扣子Token \
  -e SPRING_APPLICATION_JSON='{"risk":{"coze":{"enabled":true}}}' \
  -v /root/springboot/upload:/app/upload \
  risk-lend-pro
```

或使用外挂配置：

```bash
-v /root/springboot/config/application-prod.yaml:/app/config/application-prod.yaml
# ENTRYPOINT 增加：--spring.config.additional-location=/app/config/application-prod.yaml
```

---

## 扣子工作流侧 checklist

1. **去掉/停用** 工作流内的 Cron 触发节点（改由 Java API 触发）
2. 工作流须支持 **API 调用**（`/v1/workflow/run`）
3. 扣子环境变量（工作流内 HTTP 节点使用）：

| 变量 | 示例 |
|------|------|
| `SPRING_BASE_URL` | `http://47.109.109.231:8080` |
| `BLACKLIST_SYNC_TOKEN` | 与 Java `risk.blacklist-sync.api-token` 一致 |

4. 回调 URL：`{SPRING_BASE_URL}/api/v1/sync/blacklist`（context-path 已含 `/api/v1`）

---

## 验证

1. 启动后日志：`Supplement upload dir: ...`（无关）及 `Coze blacklist sync job started`（到点或改 Cron 后）
2. 成功：`Coze blacklist sync job finished: ok`
3. 查库：`SELECT COUNT(*), MAX(created_at) FROM credit_data_db.blacklist;`
4. Token 未配：日志 `risk.coze.token 未配置`

---

## 与现有定时任务并列

| Job | Cron | 作用 |
|-----|------|------|
| `RepaymentScheduleTask` | 01:00 | 还款计划逾期状态 |
| `OverdueLimitAdjustTask` | 02:00 | 逾期用户调额 |
| `SupplementMaterialCleanupJob` | 02:30 | 过期补充材料清理 |
| **`CozeBlacklistSyncJob`** | **02:00** | **触发扣子黑名单增量同步** |
