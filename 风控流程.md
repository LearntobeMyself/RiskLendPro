# 风控评分流程文档

## 一、整体架构

本系统采用**离线训练 + 在线评分**的分离架构：

```
┌─────────────────────────────────────────────────────────────────────┐
│                        离线处理层（Python）                          │
├─────────────────────────────────────────────────────────────────────┤
│  1. 数据准备 → 2. 模型训练 → 3. 规则生成 → 4. 规则入库                 │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ 写入数据库
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        在线服务层（Java）                            │
├─────────────────────────────────────────────────────────────────────┤
│  1. 读取规则 → 2. 用户特征获取 → 3. 评分计算 → 4. 决策输出            │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、Python 离线训练流程

### 2.1 数据来源

系统使用 **LendingClub 贷款数据集**（`data/training_data.csv`）进行训练，包含以下关键字段：

| 字段名 | 说明 | 数据示例 |
|-------|------|---------|
| `loan_status` | 贷款状态 | Charged Off / Fully Paid |
| `annual_inc` | 年收入 | 80000 |
| `inq_last_6mths` | 近6月征信查询 | 2 |
| `open_acc` | 未结清账户数 | 5 |
| `dti` | 负债收入比 | 25 |

### 2.2 数据处理

```python
# 核心处理逻辑（train_scoring_model.py）

def process_lending_club_data(df):
    # 1. 筛选有效样本
    df = df[df['loan_status'].isin(['Charged Off', 'Fully Paid'])].copy()
    
    # 2. 特征转换
    df['income'] = df['annual_inc'] / 12  # 转换为月收入
    df['credit_query_count_3m'] = (df['inq_last_6mths'] / 2).fillna(0).astype(int)
    df['multi_head_loan_count'] = df['open_acc'].fillna(0).astype(int)
    
    # 3. 标签生成（违约=1，正常=0）
    df['defaulted'] = (df['loan_status'] == 'Charged Off').astype(int)
    
    return df
```

### 2.3 特征分箱

系统对连续特征进行分箱处理，生成类别特征：

| 特征 | 分箱规则 | 标签 |
|-----|---------|-----|
| **年龄** | [0,25), [25,35), [35,50), [50,100) | age_0_25, age_26_35, ... |
| **收入** | [0,5000), [5000,15000), [15000,∞) | income_below_5000, ... |
| **多头借贷** | [-1,3], [3,6], [6,∞) | multi_head_0_3, ... |
| **征信查询** | [-1,3], [3,8], [8,∞) | credit_query_0_3, ... |
| **逾期次数** | [-1,0], [0,2], [2,∞) | overdue_12m_0, ... |
| **DTI** | [-1,15], [15,30], [30,∞) | dti_low, ... |

### 2.4 模型训练

使用 **逻辑回归** 进行训练：

```python
model = LogisticRegression(
    class_weight='balanced',  # 处理样本不平衡
    random_state=42,
    max_iter=200
)
model.fit(X_train, y_train)
```

### 2.5 评分规则生成

训练完成后，系统生成结构化的评分规则：

```json
{
  "version": "v3.0",
  "feature_weights": {
    "age": -0.1847,
    "income": -6.6138e-05,
    "overdue_count_12m": 6.3431,
    "multi_head_loan_count": -0.1243
  },
  "feature_scores": {
    "age": {
      "groups": [
        {"range": [22, 25], "score": -10, "reason": "年轻，还款能力不稳定"},
        {"range": [25, 30], "score": -5, "reason": "较年轻"},
        {"range": [30, 40], "score": 8, "reason": "黄金年龄段"}
      ]
    },
    "overdue_count_12m": {
      "groups": [
        {"range": [0, 1], "score": 0, "reason": "无逾期"},
        {"range": [1, 2], "score": -15, "reason": "有逾期记录"},
        {"range": [3, ∞], "score": -40, "reason": "严重逾期"}
      ]
    }
  },
  "thresholds": {
    "auto_approve": 75,
    "manual_review": 50
  },
  "scorecard": {
    "pdo": 80,
    "target_score": 70,
    "target_odds": 1
  }
}
```

### 2.6 规则入库

通过 `load_to_mysql.py` 将规则写入数据库：

```python
def load_scoring_rules_to_mysql():
    # 读取评分规则文件
    with open("output/scoring_rules.json", "r", encoding="utf-8") as f:
        rules = json.load(f)
    
    # 写入数据库
    cursor.execute("""
        INSERT INTO scoring_rules (
            version, rule_content, is_active, created_at
        ) VALUES (%s, %s, 1, NOW())
    """, (rules['version'], json.dumps(rules)))
```

---

## 三、Java 在线评分流程

### 3.1 规则读取

```java
private ScoringConfig loadScoringConfig() {
    ScoringConfig config = new ScoringConfig();
    ScoringRules rules = scoringRulesMapper.selectActiveRule();
    
    if (rules != null && rules.getRuleContent() != null) {
        // 解析JSON规则
        Map<String, Object> ruleMap = objectMapper.readValue(
            rules.getRuleContent(),
            new TypeReference<Map<String, Object>>() {}
        );
        
        // 获取特征评分规则
        config.featureScores = (Map<String, Object>) ruleMap.get("feature_scores");
        
        // 获取决策阈值
        Map<String, Object> thresholds = (Map<String, Object>) ruleMap.get("thresholds");
        config.autoApproveThreshold = ((Number) thresholds.get("auto_approve")).intValue();
        config.manualReviewThreshold = ((Number) thresholds.get("manual_review")).intValue();
    }
    
    return config;
}
```

### 3.2 评分计算流程

#### 步骤1：基础分初始化

```java
int score = 100;  // 基础分
List<ScoreContribution> contributions = new ArrayList<>();
contributions.add(new ScoreContribution(
    "基础分", "基础分", 100.0, 100.0, "信用评估基础分"
));
```

#### 步骤2：个人信息评分

| 特征 | 评分规则 | 代码位置 |
|-----|---------|---------|
| **年龄** | 22-25岁:-10, 25-30岁:-5, 30-40岁:+8, 40-50岁:+5, 50+岁:-3 | `calculateAgeScore()` |
| **月收入** | <3000:-15, 3000-8000:-5, 8000-15000:+5, >15000:+15 | `calculateIncomeScore()` |
| **工作类型** | 公务员:+15, 企事业单位:+10, 私营企业:0, 其他:-5 | `calculateJobScore()` |
| **学历** | 博士:+15, 硕士:+10, 本科:+5, 高中及以下:-5 | `calculateEducationScore()` |
| **房产** | 有:+10, 无:0 | `calculateHouseScore()` |
| **车产** | 有:+5, 无:0 | `calculateCarScore()` |
| **婚姻** | 已婚:+5, 未婚:0 | `calculateMarriageScore()` |

#### 步骤3：外部征信评分

| 特征 | 评分规则 | 代码位置 |
|-----|---------|---------|
| **多头借贷** | 0-3:0, 3-6:-10, 6-10:-20, >10:-30 | `calculateMultiHeadScore()` |
| **征信查询** | 0-3:0, 3-6:-5, 6-10:-10, >10:-15 | `calculateQueryScore()` |
| **逾期次数** | 0:0, 1:-15, 2:-25, >=3:-40 | `calculateOverdueScore()` |
| **DTI** | <15:+5, 15-30:0, >30:-15 | `calculateDtiScore()` |
| **外部信用分** | >=800:+10, >=700:+5, <600:-10 | `calculateExternalCreditScore()` |

#### 步骤4：反欺诈检测

| 检测项 | 扣分 |
|-------|-----|
| 虚拟设备 | -20 |
| 代理IP | -10 |

#### 步骤5：分数归一化

```java
// 限制分数在10-90之间，避免极端值
score = Math.max(10, Math.min(90, score));
```

### 3.3 决策输出

```java
@Override
public String getDecision(int score) {
    ScoringConfig config = loadScoringConfig();
    if (score >= config.autoApproveThreshold) {
        return "APPROVE";   // 自动通过
    } else if (score >= config.manualReviewThreshold) {
        return "REVIEW";    // 人工审核
    } else {
        return "REJECT";    // 拒绝
    }
}
```

---

## 四、评分规则数据结构

### 4.1 数据库表结构

**表名**: `scoring_rules`

| 字段 | 类型 | 说明 |
|-----|------|-----|
| `id` | BIGINT | 主键 |
| `version` | VARCHAR(20) | 规则版本号 |
| `rule_content` | TEXT | 完整规则JSON |
| `is_active` | TINYINT | 是否启用 |
| `created_at` | DATETIME | 创建时间 |

### 4.2 rule_content 结构详解

```json
{
  "version": "v3.0",
  "description": "基于LendingClub数据训练的评分规则",
  
  "feature_weights": { ... },      // 模型原始权重（逻辑回归系数）
  "intercept": -0.0202,           // 截距项
  
  "feature_scores": {              // 业务评分规则（用于在线评分）
    "age": {
      "description": "年龄评分",
      "groups": [
        {"range": [min, max], "score": int, "reason": "说明"}
      ]
    },
    "job_type": {
      "description": "工作类型评分",
      "scores": {
        "公务员": 15,
        "企事业单位": 10
      }
    },
    "has_house": {
      "description": "房产评分",
      "has": 10,
      "none": 0
    }
  },
  
  "thresholds": {
    "auto_approve": 75,            // 自动通过阈值
    "manual_review": 50            // 人工审核阈值
  },
  
  "scorecard": {                   // 评分卡参数（备用）
    "base_score": 600,
    "pdo": 80,
    "target_score": 70,
    "target_odds": 1
  },
  
  "training_data": {               // 训练数据统计
    "total_count": 39786,
    "default_rate": 0.1397
  },
  
  "model_metrics": {               // 模型评估指标
    "accuracy": 0.85,
    "auc": 0.88
  }
}
```

---

## 五、评分示例分析

### 5.1 测试案例1：中等风险用户

**用户信息**:
- 年龄：27岁
- 月收入：8000-15000
- 工作类型：私营企业
- 学历：本科
- 未婚，无房无车

**外部征信**:
- 多头借贷：5家
- 征信查询：3次
- 逾期次数：3次
- 外部信用分：614

**评分计算**:

| 特征 | 贡献值 | 说明 |
|-----|-------|------|
| 基础分 | +100 | 初始分 |
| 年龄(27) | -5 | 青年时期 |
| 月收入 | +5 | 收入中等 |
| 工作类型 | 0 | 私营企业 |
| 学历 | +5 | 本科学历 |
| 多头借贷(5) | -10 | 中等多头 |
| 征信查询(3) | -5 | 查询较多 |
| 逾期(3) | -40 | 严重逾期 |
| **合计** | **50** | REVIEW |

**决策**: REVIEW（人工审核）

### 5.2 测试案例2：高信用用户

**用户信息**:
- 年龄：41岁
- 月收入：15000以上
- 工作类型：企事业单位
- 学历：硕士
- 已婚，有房有车

**外部征信**:
- 多头借贷：7家
- 征信查询：1次
- 逾期次数：1次
- 外部信用分：780

**评分计算**:

| 特征 | 贡献值 | 说明 |
|-----|-------|------|
| 基础分 | +100 | 初始分 |
| 年龄(41) | +5 | 稳定年龄段 |
| 月收入 | +15 | 收入较高 |
| 工作类型 | +10 | 企事业单位 |
| 学历 | +10 | 硕士 |
| 房产 | +10 | 有房产 |
| 车产 | +5 | 有车产 |
| 婚姻 | +5 | 已婚 |
| 多头借贷(7) | -20 | 高多头 |
| 征信查询(1) | 0 | 正常 |
| 逾期(1) | -15 | 有逾期记录 |
| 外部信用分 | +5 | 信用良好 |
| **合计** | **100** → **90** | APPROVE |

**决策**: APPROVE（自动通过）

---

## 六、常见问题解答

### 6.1 为什么有些特征贡献为0？

**原因分析**:

| 情况 | 说明 | 示例 |
|-----|------|-----|
| **规则未匹配** | 数据库中无对应规则，使用默认值 | DTI为null时 |
| **特征值为0** | 某些二元特征的"无"状态 | 无房产、无车产 |
| **规则定义为0** | 业务规则中该范围评分为0 | 私营企业、未婚 |
| **查询次数正常** | 0-3次查询为正常范围 | credit_query_count_3m=1 |

**代码逻辑**:

```java
private int calculateHouseScore(Boolean hasHouse, Map<String, Object> featureScores) {
    if (featureScores == null) {
        return Boolean.TRUE.equals(hasHouse) ? 10 : 0;  // 无房产返回0
    }
    // ...
}
```

### 6.2 为什么使用业务规则而非模型权重？

| 对比项 | 模型权重 | 业务规则 |
|-------|---------|---------|
| **可解释性** | 难以理解 | 清晰易懂 |
| **可调整性** | 需重新训练 | 直接修改规则 |
| **业务对齐** | 纯数据驱动 | 结合业务经验 |
| **极端值处理** | 可能产生极端分数 | 可控范围 |

### 6.3 评分卡参数的作用

| 参数 | 说明 | 当前值 |
|-----|------|-------|
| **PDO** | 赔率翻倍所需分数 | 80 |
| **target_score** | 目标分数 | 70 |
| **target_odds** | 目标赔率 | 1 |
| **分数范围** | 最终分数限制 | 10-90 |

---

## 七、运行流程

### 7.1 Python端操作

```bash
# 1. 生成模拟数据
python generate_mock_data.py

# 2. 清洗数据
python clean_blacklist.py
python clean_user_features.py

# 3. 写入数据库
python load_to_mysql.py

# 4. 训练模型并生成规则
python train_scoring_model.py
```

### 7.2 Java端操作

```bash
# 编译并启动服务
cd d:\javacode\RiskLendPro
mvn clean compile
mvn spring-boot:run
```

### 7.3 API调用示例

```bash
# 风险评估接口
curl -X POST http://localhost:8080/api/risk/assess \
  -H "Content-Type: application/json" \
  -d '{
    "idCard": "510106199903170001",
    "name": "测试用户",
    "birthday": "1999-03-17",
    "monthlyIncome": "8000-15000",
    "jobType": "私营企业",
    "education": "本科",
    "marriage": "未婚",
    "hasHouse": false,
    "hasCar": false
  }'
```

---

## 八、代码目录结构

```
RiskLendPro/
├── risk-assessment/                    # Python离线处理模块
│   ├── generate_mock_data.py          # 生成模拟数据
│   ├── clean_blacklist.py             # 清洗黑名单
│   ├── clean_user_features.py         # 清洗用户特征
│   ├── load_to_mysql.py               # 数据入库
│   ├── train_scoring_model.py         # 模型训练
│   ├── data/
│   │   └── training_data.csv          # LendingClub训练数据
│   └── output/
│       └── scoring_rules.json         # 生成的评分规则
└── src/main/java/                     # Java在线服务模块
    └── org/example/risklendpro/
        ├── service/impl/
        │   └── CreditScoreEngineImpl.java  # 评分引擎实现
        ├── mapper/credit/
        │   ├── ScoringRulesMapper.java     # 规则读取Mapper
        │   └── UserExternalFeaturesMapper.java  # 特征读取Mapper
        └── entity/credit/
            ├── ScoringRules.java           # 规则实体
            └── UserExternalFeatures.java   # 特征实体
```

---

## 九、版本历史

| 版本 | 日期 | 修改内容 |
|-----|------|---------|
| v1.0 | 2024-01 | 初始版本，使用模型权重直接评分 |
| v2.0 | 2024-02 | 引入评分卡转换，修复极端分数问题 |
| v3.0 | 2024-03 | 改用业务规则评分，增加特征分析，优化分数范围(10-90) |

---

**文档版本**: v1.0  
**生成时间**: 2026年5月  
**适用系统**: RiskLendPro 风控评分系统