# RiskLendPro 风控评分系统 - 项目总览

***

## 一、项目概述

### 1.1 项目定位

**RiskLendPro** 是一个基于 **Java + Python** 的互联网个人贷款风控评分系统，采用**离线训练 + 在线评分**的分离架构，用于贷款申请的自动化风险评估和授信决策。

### 1.2 核心目标

| 目标        | 说明                                        |
| --------- | ----------------------------------------- |
| **自动化决策** | 根据用户信息和外部征信数据自动评估贷款风险                     |
| **数据驱动**  | 基于真实业务数据（LendingClub + Home Credit）训练评分规则 |
| **可解释性**  | 提供详细的评分明细和决策依据                            |
| **灵活扩展**  | 支持规则动态更新，无需重启服务                           |

### 1.3 技术架构

```
┌─────────────────────────────────────────────────────────────────────┐
│                        离线处理层（Python）                          │
├─────────────────────────────────────────────────────────────────────┤
│  数据准备 → 数据清洗 → 模型训练 → 规则生成 → 规则入库                     │
│  (CSV分析)   (黑名单/特征)   (逻辑回归)   (业务规则)   (MySQL)           │
└───────────────────────────┬─────────────────────────────────────────┘
                            │ 写入数据库
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        在线服务层（Java）                            │
├─────────────────────────────────────────────────────────────────────┤
│  用户注册/登录 → 风控评估 → 黑名单检查 → 数据核验 → 评分计算 → 决策输出    │
│  (JWT认证)     (申请提交)   (三级匹配)   (收入偏差)   (评分卡)   (APPROVE/REVIEW/REJECT) │
└─────────────────────────────────────────────────────────────────────┘
```

### 1.4 项目结构

```
RiskLendPro/
├── src/main/java/org/example/risklendpro/  # Java后端代码
│   ├── controller/        # REST API控制器
│   ├── service/           # 业务逻辑层
│   ├── mapper/            # 数据访问层
│   ├── entity/            # 数据库实体类
│   ├── config/            # 配置类
│   ├── pojo/              # 请求/响应对象
│   ├── enums/             # 枚举类
│   └── utils/             # 工具类
├── src/main/resources/     # 资源文件
│   └── mapper/            # MyBatis映射文件
├── risk-assessment/       # Python离线处理模块
│   ├── data/              # 数据目录
│   │   ├── raw/           # 原始数据
│   │   └── cleaned/       # 清洗后数据
│   ├── output/            # 输出目录
│   └── *.py               # Python脚本
├── sql/                   # SQL脚本和测试用例
└── *.md                   # 文档文件
```

### 1.5 参考文档

| 文档                | 路径                          | 说明           |
| ----------------- | --------------------------- | ------------ |
| **详细风控流程**        | `详细风控流程.md`                 | 评分流程、规则说明    |
| **接口文档**          | `接口文档.md`                   | API定义、请求响应示例 |
| **数据库设计**         | `java端实体类与数据库设计.md`         | 数据库表结构、实体类   |
| **CSV数据说明**       | `csv数据来源和数据库表设计.md`         | 数据源字段、统计信息   |
| **Python README** | `risk-assessment/README.md` | Python模块使用说明 |

***

## 二、数据层设计

### 2.1 数据源

**主数据源**：

| 数据源             | 来源          | 说明                  |
| --------------- | ----------- | ------------------- |
| **LendingClub** | Kaggle竞赛数据集 | 42,500+条贷款记录，用于模型训练 |
| **Home Credit** | HuggingFace | 用户外部特征数据，500条抽样     |
| **失信被执行人**      | 温州政府公开数据    | 200条黑名单记录（有地区编码）    |

**样本分布**：

- LendingClub：正常还款 85% vs 违约 15%
- 黑名单：失信被执行人 200条（有地区编码）
- 用户特征：按地区110112和310000各抽取250条

### 2.2 关键字段映射

| 数据源字段            | 模型字段                    | 转换方式                        |
| ---------------- | ----------------------- | --------------------------- |
| `annual_inc`     | `income`                | 除以12得到月收入                   |
| `inq_last_6mths` | `credit_query_count_3m` | 乘以0.5估算                     |
| `open_acc`       | `active_loans_count`    | 直接使用                        |
| `delinq_2yrs`    | `target`                | 历史逾期标签                      |
| `EXT_SOURCE_2/3` | `ext_source_2/3`        | 第三方评分直接使用                   |
| `loan_status`    | `defaulted`             | Charged Off→1, Fully Paid→0 |

### 2.3 数据库表结构

**主数据库表（RiskLendPro）**：

| 表名                  | 说明         | 关键字段                                                           |
| ------------------- | ---------- | -------------------------------------------------------------- |
| `user`              | 用户表        | `id`, `real_name`, `phone_number`, `id_card`, `assessment_status` |
| `admin`             | 管理员表       | `id`, `username`, `password`, `phone_number`, `email`          |
| `risk_assessment`   | 风控评估表      | `apply_id`, `user_id`, `status`, `total_score`, `credit_limit`, `is_final`, `sys_decision` |
| `loan`              | 借款记录表      | `loan_id`, `user_id`, `amount`, `status`, `repayment_method`, `auto_approved` |
| `repayment_plan`    | 还款计划表      | `plan_id`, `loan_id`, `total_amount`, `overdue_level`, `current_period` |
| `repayment_record`  | 还款记录表      | `record_id`, `plan_id`, `period`, `amount`, `status`, `due_date` |
| `user_credit_limit` | 用户额度表      | `user_id`, `total_limit`, `used_limit`, `remaining_limit`, `has_overdue` |
| `limit_adjust_log`  | 额度调整日志     | `user_id`, `old_limit`, `new_limit`, `reason`, `operator_id`   |
| `vintage_data`      | Vintage数据表 | `month`, `disbursed_amount`, `m1_rate`, `m2_rate`, `m3_rate`   |

**风控数据库表（credit_data_db）**：

| 表名                       | 说明     | 关键字段                                                         |
| ------------------------ | ------ | ------------------------------------------------------------ |
| `blacklist`              | 黑名单表   | `name`, `area_code`, `birth_year`, `risk_level`, `case_no`, `duty_status` |
| `user_external_features` | 用户外部特征 | `sk_id_curr`, `id_card`, `amt_income_total`, `ext_source_2/3`, `target`, `prev_refused_count` |
| `scoring_rules`          | 评分规则表  | `version`, `feature_weights`, `scorecard`, `threshold_auto_approve`, `threshold_manual_review` |

### 2.4 实体类对应关系

| 实体类                    | 对应表                      | 说明       |
| ---------------------- | ------------------------ | -------- |
| `User`                 | `user`                   | 用户实体     |
| `Admin`                | `admin`                  | 管理员实体    |
| `RiskAssessment`       | `risk_assessment`        | 风控评估实体   |
| `Loan`                 | `loan`                   | 借款实体     |
| `RepaymentPlan`        | `repayment_plan`         | 还款计划实体   |
| `RepaymentRecord`      | `repayment_record`       | 还款记录实体   |
| `UserCreditLimit`      | `user_credit_limit`      | 用户额度实体   |
| `LimitAdjustLog`       | `limit_adjust_log`       | 额度调整日志实体 |
| `VintageData`          | `vintage_data`           | Vintage数据实体 |
| `Blacklist`            | `blacklist`              | 黑名单实体    |
| `UserExternalFeatures` | `user_external_features` | 用户外部特征实体 |
| `ScoringRules`         | `scoring_rules`          | 评分规则实体   |

***

## 三、评分规则体系

### 3.1 评分维度

| 维度        | 特征                    | 评分影响       |
| --------- | --------------------- | ---------- |
| **个人信息**  | 年龄、学历、婚姻、职业、收入、资产     | -15 \~ +15 |
| **外部征信**  | 活跃贷款数、征信查询、历史逾期、第三方评分 | -40 \~ +5  |
| **黑名单匹配** | 姓名、地域、出生年份三级匹配        | 直接拒绝/人工审核  |

### 3.2 决策规则

| 评分区间      | 决策             | 说明          |
| --------- | -------------- | ----------- |
| ≥ 720     | APPROVE        | 自动通过，生成授信额度 |
| 580 - 719 | MANUAL_REVIEW  | 人工审核        |
| < 580     | REJECT         | 系统拒绝        |

### 3.3 黑名单三级匹配规则

| 匹配等级 | 命中因子           | 风险等级 | 决策     | 业务解释           |
| ---- | -------------- | ---- | ------ | -------------- |
| 全不中 | 姓名未命中          | 安全   | 自动通过   | 数据库无记录         |
| 一级   | 仅姓名命中          | 低风险  | 通过（标记） | 全国重名极多，忽略不计   |
| 二级   | 姓名 + 地域        | 中风险  | 人工审核   | 同城重名可能存在，需核实  |
| 三级   | 姓名 + 地域 + 出生年份 | 高风险  | 直接拒绝   | 极大概率为本人，重名概率极低 |

**支持通配符模式**（如 `李*梅` 可匹配 `李小微`、`李红梅` 等）

### 3.4 数据核验规则（收入偏差检测）

| 偏差范围           | 处理方式 | 风险标签           | 说明        |
| -------------- | ---- | -------------- | --------- |
| 实际收入在自填区间内     | 自动通过 | -              | 数据一致      |
| 偏差 ≤ 15%       | 自动通过 | INCOME_TOLERANCE | 小幅偏差，取后台值 |
| 15% < 偏差 ≤ 50% | 人工审核 | INCOME_OUTLIER   | 中等偏差需核实   |
| 偏差 > 50%       | 自动拒绝 | -              | 严重虚报      |

### 3.5 评分规则输出格式

```json
{
  "version": "v1.0",
  "feature_weights": {
    "ext_source_2": 0.8,
    "ext_source_3": 0.6,
    "age": 0.3,
    "active_loans_count": -0.5
  },
  "scorecard": {
    "ext_source_2": {"0-0.3": 0, "0.3-0.6": 20, "0.6-1.0": 40}
  },
  "thresholds": {
    "auto_approve": 720,
    "manual_review": 580
  },
  "intercept": 0.5,
  "model_metrics": {
    "accuracy": 0.78,
    "auc": 0.85
  }
}
```

***

## 四、核心流程

### 4.1 Python 离线处理流程

```
1. 数据准备
   ├── 下载原始数据（LendingClub、Home Credit、黑名单）
   └── 复制到 risk-assessment/data/

2. 数据清洗
   ├── clean_blacklist.py → 提取200条有地区编码的黑名单
   └── clean_user_features.py → 按地区110112和310000各抽取250条，生成身份证号

3. 模型训练
   ├── train_scoring_model.py → 训练逻辑回归模型
   └── 生成 scoring_rules.json

4. 数据入库
   └── load_to_mysql.py → 写入 credit_data_db 数据库
```

### 4.2 Java 在线风控流程

```
用户提交申请
    ↓
身份校验（用户存在性、身份一致性、年龄≥18）
    ↓
三级黑名单检查（姓名+地域+出生年份，支持通配符）
    ↓
数据核验（收入偏差检测）
    ↓
重复申请检查（30天冷却期）
    ↓
风控评分计算（逻辑回归 + 评分卡）
    ↓
结果判定（APPROVE/MANUAL_REVIEW/REJECT）
    ↓
邮件通知 + Redis缓存报告
```

### 4.3 状态流转

```
WAITING (提交后)
    ↓
├── APPROVE → FINAL_PASS (通过，设置额度，有效期1年)
├── MANUAL_REVIEW → MANUAL_REVIEW (待人工审核)
└── REJECT → SYSTEM_REJECT (拒绝，额度为0)

MANUAL_REVIEW
    ↓
├── 人工通过 → FINAL_PASS (设置额度)
└── 人工拒绝 → FINAL_REJECT
```

### 4.4 借款审批流程

```
用户发起借款
    ↓
检查逾期状态
    ↓
检查借款金额 vs 剩余额度
    ↓
├── 金额 ≤ 额度 → 自动审批 → DISBURSED → 扣减额度 → 放款
└── 金额 > 额度 → PENDING_APPROVAL → 管理员审批
```

***

## 五、Java端API接口

### 5.1 认证模块

| 接口                | 方法   | 说明    | 是否需要认证 |
| ----------------- | ---- | ----- | ------ |
| `/auth/register`  | POST | 用户注册  | 否      |
| `/auth/login`     | POST | 用户登录  | 否      |
| `/admin/register` | POST | 管理员注册 | 否      |
| `/admin/login`    | POST | 管理员登录 | 否      |

### 5.2 风控评估模块

| 接口                        | 方法   | 说明       |
| ------------------------- | ---- | -------- |
| `/risk/assessment/submit` | POST | 提交风控评估申请 |
| `/risk/assessment/status` | GET  | 查询评估状态   |
| `/risk/assessment/result` | GET  | 获取最终额度结果 |

### 5.3 借款模块

| 接口                        | 方法   | 说明         |
| ------------------------- | ---- | ---------- |
| `/loan/request`           | POST | 发起借款请求     |
| `/loan/user/history`      | GET  | 获取用户借款记录   |
| `/loan/user/applications` | GET  | 获取用户贷款申请记录 |

### 5.4 还款模块

| 接口                           | 方法   | 说明       |
| ---------------------------- | ---- | -------- |
| `/repayment/plans`           | GET  | 查看还款计划   |
| `/repayment/execute`         | POST | 执行还款     |
| `/repayment/record/{planId}` | GET  | 获取还款记录详情 |

### 5.5 管理员模块

| 接口                             | 方法   | 说明        |
| ------------------------------ | ---- | --------- |
| `/admin/risk/list`             | GET  | 获取待审批列表   |
| `/admin/risk/report/{applyId}` | GET  | 获取详细风控报告  |
| `/admin/risk/approve`          | POST | 管理员审批决策   |
| `/admin/loan/pending-list`     | GET  | 获取待审批贷款申请 |
| `/admin/loan/approve`          | POST | 管理员审批贷款   |
| `/admin/dashboard/stats`       | GET  | 获取首页统计    |

### 5.6 请求响应示例

**提交风控评估申请：**

请求：

```json
{
  "idCard": "110101199012103003",
  "name": "张三",
  "phone": "13800138001",
  "email": "zhangsan@example.com",
  "gender": 1,
  "birthday": "1990-12-10",
  "education": "本科",
  "marriage": "已婚",
  "jobType": "企事业单位",
  "monthlyIncome": "15000以上",
  "hasHouse": true,
  "hasCar": true,
  "contactPhone": "13800138000"
}
```

响应：

```json
{
  "code": 200,
  "message": "申请已受理，风控评估启动",
  "success": true,
  "data": {
    "applyId": "L1672531200000abcd",
    "submitTime": "2026-05-09T10:00:00",
    "status": "WAITING"
  }
}
```

**查询评估状态：**

请求：`GET /risk/assessment/status?applyId=L1672531200000abcd`

响应：

```json
{
  "applyId": "L1672531200000abcd",
  "status": "FINAL_PASS",
  "statusTitle": "已通过",
  "final": true
}
```

**管理员待审批列表：**

请求：`GET /admin/risk/list?page=1&size=10`

响应：

```json
{
  "code": 200,
  "data": {
    "current": 1,
    "records": [
      {
        "applyId": "L1672531200000abcd",
        "userName": "吴小微",
        "phone": "138****8001",
        "idCard": "110**********1001",
        "totalScore": 650,
        "sysDecision": "MANUAL_REVIEW",
        "status": "MANUAL_REVIEW",
        "applyTime": "2026-05-08T21:30:00",
        "auditRemark": "BLACKLIST_MATCH_LEVEL_2: 姓名+地域命中黑名单",
        "riskTags": ["姓名+地域命中黑名单"]
      }
    ]
  }
}
```

***

## 六、运行指南

### 6.1 Python 端

```bash
cd risk-assessment

# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置环境变量（修改 .env 文件）
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=admin
# DB_PASSWORD=admin
# DB_NAME=credit_data_db

# 3. 数据清洗
python clean_blacklist.py    # 清洗黑名单（保留200条有地区编码记录）
python clean_user_features.py # 清洗用户特征（按地区抽样500条，生成身份证号）

# 4. 训练模型
python train_scoring_model.py

# 5. 数据入库
python load_to_mysql.py
```

### 6.2 Java 端

```bash
cd d:\javacode\RiskLendPro

# 编译
mvn clean compile

# 运行
mvn spring-boot:run

# 服务地址: http://localhost:8080
```

### 6.3 配置说明

**application.yml 关键配置：**

```yaml
spring:
  datasource:
    primary:
      jdbc-url: jdbc:mysql://localhost:3306/RiskLendPro
    credit:
      jdbc-url: jdbc:mysql://localhost:3306/credit_data_db
  redis:
    host: localhost
    port: 6379
```

***

## 七、关键设计要点

### 7.1 数据驱动设计

| 数据特点         | 设计应用                        |
| ------------ | --------------------------- |
| 样本不平衡(85:15) | 使用 class_weight='balanced' |
| 特征相关性分析      | 逾期权重最高，第三方评分次之              |
| 真实违约分布       | 评分区间根据违约率确定                 |

### 7.2 规则更新机制

```
Python训练 → scoring_rules.json → MySQL → Java动态读取
                                          │
                                          ▼
                                   无需重启服务
                                   规则实时生效
```

### 7.3 容错机制

| 场景     | 处理方式      |
| ------ | --------- |
| 数据库无规则 | 使用硬编码兜底规则 |
| 特征数据缺失 | 对应项评分为0   |
| 评估异常   | 自动设置为拒绝   |

### 7.4 安全设计

| 安全措施  | 说明           |
| ----- | ------------ |
| JWT认证 | 无状态身份认证      |
| 密码加密  | BCrypt加密存储   |
| 数据脱敏  | 手机号、身份证号脱敏展示 |
| 权限控制  | 用户/管理员角色分离   |

***

## 八、测试用例概览

### 8.1 黑名单测试用例

| 测试场景     | 姓名  | 身份证号               | 匹配等级 | 预期结果   | 说明                     |
| -------- | --- | ------------------ | ---- | ------ | ---------------------- |
| 三级匹配（拒绝） | 吴小微 | 110112197705151001 | 三级   | 系统拒绝   | 姓名+地域+出生年份完全命中         |
| 二级匹配（人工） | 方小巢 | 310000198408202002 | 二级   | 人工审核   | 姓名+地域命中，出生年份不匹配       |
| 一级匹配（通过） | 张小梁 | 110101199012103003 | 一级   | 通过（标记） | 仅姓名命中，地区不同（高信用用户）   |
| 未命中（通过）  | 赵六六 | 110101199506154004 | 全不中  | 自动通过   | 数据库无匹配记录             |

### 8.2 评分卡测试用例

| 测试场景  | 用户  | 用户特征                              | 预期结果 |
| ----- | --- | --------------------------------- | ---- |
| 信用分低  | 张三三 | ext_source_2=0.2629, ext_source_3=0.1394, 有逾期记录 | 系统拒绝 |
| 信用分中等 | 刘五五 | ext_source_2=0.7143, ext_source_3=0.5407, 无逾期记录, 职业为经理 | 人工审核 |

### 8.3 数据核验测试用例

| 测试场景   | 自填收入       | 后台收入  | 偏差   | 预期结果 |
| ------ | ---------- | ----- | ---- | ---- |
| 偏差>50% | 15000以上    | 5625  | 167% | 自动拒绝 |
| 偏差30%  | 15000以上    | 11250 | 33%  | 人工审核 |
| 偏差≤15% | 8000-15000 | 11250 | 2.2% | 自动通过 |
| 区间内   | 5000-8000  | 6000  | 0%   | 自动通过 |

***

## 九、版本历史

| 版本   | 日期   | 说明                    |
| ---- | ---- | --------------------- |
| v1.0 | 初始版本 | 基础评分功能                |
| v2.0 | -    | 引入评分卡转换               |
| v3.0 | -    | 业务规则评分，特征分析，分数范围10-90 |
| v4.0 | -    | 新增三级黑名单匹配、收入偏差检测      |
| v5.0 | 当前   | 优化数据入库逻辑，修复NaN处理和重复数据问题 |

***

**文档版本**: v1.0\
**生成时间**: 2026年5月\
**适用系统**: RiskLendPro 风控评分系统