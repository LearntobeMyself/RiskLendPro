spring:
  application:
    name: risk-service

server:
  port: ${RISK_SERVICE_PORT:8082}

service:
  user:
    url: ${USER_SERVICE_URL:http://localhost:8081}
  loan:
    url: ${LOAN_SERVICE_URL:http://localhost:8083}

management:
  endpoints:
    web:
      exposure:
        include: health,info
