spring:
  application:
    name: cs-service
  config:
    import: classpath:application-common.yml

server:
  port: ${CS_SERVICE_PORT:8084}

management:
  endpoints:
    web:
      exposure:
        include: health,info
