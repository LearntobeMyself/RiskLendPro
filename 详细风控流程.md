
# RiskLendPro 风控评估流程详解

---

## 一、风控流程总览

```
用户提交申请
    ↓
用户身份校验
    ↓
三级黑名单检查（姓名+地域+出生年份）
    ↓
数据核验（收入偏差检测）
    ↓
重复申请检查
    ↓
风控评分计算
    ↓
结果判定与通知
```

---

## 二、各阶段详细说明

### 阶段1：用户身份校验

| 检查项 | 校验逻辑 | 失败提示 |
|--------|----------|----------|
| 用户存在性 | 查询 user 表确认用户 | `用户不存在` |
| 身份一致性 | 请求身份证号与用户记录一致 | `身份信息不一致，请使用本人身份信息申请` |
| 年龄限制 | 年龄 >= 18 周岁 | `年龄不足无法提供服务` |

### 阶段2：三级黑名单检查

采用**姓名+地域+出生年份**三级匹配规则：

| 匹配等级 | 命中因子 | 风险判定 | 决策动作 | 业务解释 |
|----------|----------|----------|----------|----------|
| 全不中 | 姓名未命中 | 安全 | **自动通过** | 数据库无记录 |
| 一级弱匹配 | 仅姓名命中 | 低风险 | **通过/标记** | 全国重名极多，忽略不计 |
| 二级中匹配 | 姓名 + 地域一致 | 中风险 | **人工审批** | 同城重名可能存在，需核实 |
| 三级强匹配 | 姓名 + 地域 + 出生年份一致 | 高风险 | **直接拒绝** | 极大概率为本人，重名概率极低 |

**黑名单姓名匹配支持通配符模式**（如 `李*梅` 可匹配 `李小微`、`李红梅` 等）

### 阶段3：数据核验（收入偏差检测）

采用**分级核验逻辑**，对比用户自填收入与第三方数据：

| 偏差范围 | 处理方式 | 风险标签 | 业务说明 |
|----------|----------|----------|----------|
| ≤ 15% | 自动通过，取最小值进入模型 | `INCOME_TOLERANCE` | 偏差在容错区间内，已记录供管理员查看 |
| 15% ~ 50% | 转人工审批 | `INCOME_OUTLIER` | 收入异常，需管理员核实工资流水 |
| > 50% | 自动拒绝 | - | 判定恶意造假 |

**拒绝提示**：`抱歉，由于您的身份核验未通过，请核对信息后重新提交或联系客服。`

### 阶段4：重复申请检查

| 状态 | 处理方式 | 提示信息 |
|------|----------|----------|
| `FINAL_PASS` | 拒绝提交 | `您的评估申请已通过，请勿重复提交` |
| `WAITING` / `MANUAL_REVIEW` | 拒绝提交 | `您已有正在处理中的评估申请，请等待处理完成` |
| 其他状态且距上次申请 < 30天 | 拒绝提交 | `建议您保持良好的信用记录，X天后可尝试再次申请` |

### 阶段5：风控评分计算

调用 `CreditScoreEngine.getScoreDetailReport()` 计算评分，包含：
- 逻辑回归评分卡计算
- 用户外部特征评分（活跃贷款数、征信查询、历史标签等）
- 信用额度计算（考虑外部特征惩罚）

### 阶段6：结果判定

| 系统决策 | 状态 | 是否最终 | 额度处理 |
|----------|------|----------|----------|
| `APPROVE` | `FINAL_PASS` | 是 | 设置额度，有效期1年 |
| `MANUAL_REVIEW` | `MANUAL_REVIEW` | 否 | 等待人工审核 |
| `REJECT` | `SYSTEM_REJECT` | 是 | 额度为0 |

### 阶段2补充：管理员查看待审批内容

当申请进入人工审批流程时，管理员通过以下方式查看：

**1. 查询待审批列表**
- API：`GET /admin/risk/list`
- 参数：`page`、`size`、`status`（默认 `MANUAL_REVIEW`）
- 返回示例：
```json
{
  "applyId": "L1672531200000abcd",
  "userName": "吴小微",
  "phone": "138****8001",
  "idCard": "110**********1001",
  "totalScore": 650,
  "sysDecision": "MANUAL_REVIEW",
  "status": "MANUAL_REVIEW",
  "applyTime": "2026-05-08T21:30:00",
  "auditRemark": "BLACKLIST_MATCH_LEVEL_2: 姓名+地域命中黑名单",
  "riskTags": ["姓名+地域命中黑名单"]
}
```

**2. 风险标签说明**

| 风险标签 | 触发条件 | 处理建议 |
|----------|----------|----------|
| `姓名+地域命中黑名单` | 二级中匹配（姓名+地域一致） | 要求用户提供征信报告核实 |
| `仅姓名命中黑名单` | 一级弱匹配（仅姓名命中） | 标记后可继续流程 |
| `收入异常(需人工复核)` | 收入偏差15%-50% | 要求上传近3个月工资流水 |
| `收入偏差(已自动通过)` | 收入偏差≤15% | 仅记录，无需处理 |

**3. 详细风控报告**
- API：`GET /admin/risk/report/{applyId}`
- 通过 applyId 查询 Redis 中的详细评分报告

---

## 三、返回结果汇总

### 3.1 submit() 方法返回结果

**成功场景**：
```json
{
  "applyId": "L1672531200000abcd",
  "submitTime": "2026-05-08T21:30:00",
  "status": "FINAL_PASS"  // 或 "SYSTEM_REJECT"、"MANUAL_REVIEW"
}
```

**异常场景**（RuntimeException）：

| 错误信息 | 触发条件 |
|----------|----------|
| 用户不存在 | 用户ID无效 |
| 身份信息不一致，请使用本人身份信息申请 | 请求身份证号与用户记录不匹配 |
| 年龄不足无法提供服务 | 年龄 < 18 |
| 由于您的综合信用评估不符合要求，暂无法提供服务 | 黑名单三级强匹配命中 |
| 您的申请需要人工复核，请耐心等待 | 黑名单二级中匹配 / 收入偏差15%-50% |
| 抱歉，由于您的身份核验未通过，请核对信息后重新提交或联系客服。 | 收入偏差 > 50% |
| 您的评估申请已通过，请勿重复提交 | 已有通过记录 |
| 您已有正在处理中的评估申请，请等待处理完成 | 已有处理中申请 |
| 建议您保持良好的信用记录，X天后可尝试再次申请 | 距上次申请 < 30天 |

### 3.2 getStatus() 方法返回结果

**成功场景**：
```json
{
  "applyId": "L1672531200000abcd",
  "status": "FINAL_PASS",
  "statusTitle": "已通过",
  "final": true
}
```

**状态标题映射**：

| status | statusTitle |
|--------|-------------|
| `WAITING` | 评估中 |
| `SYSTEM_REJECT` | 系统拒绝 |
| `MANUAL_REVIEW` | 人工复核中 |
| `FINAL_PASS` | 已通过 |
| `FINAL_REJECT` | 已拒绝 |

**异常场景**：
- `评估申请不存在`：applyId 无效
- `评估尚未完成`：isFinal = false（仅 getResult()）

### 3.3 getResult() 方法返回结果

**成功场景**：
```json
{
  "applyId": "L1672531200000abcd",
  "totalScore": 750,
  "creditLimit": 50000,
  "expireDate": "2027-05-08",
  "status": "FINAL_PASS",
  "approvalTime": "2026-05-08T21:30:00"
}
```

**异常场景**：
- `评估申请不存在`：applyId 无效
- `评估尚未完成`：isFinal = false

---

## 四、数据库状态流转

```
WAITING (提交后)
    ↓
├── APPROVE → FINAL_PASS (通过)
├── MANUAL_REVIEW → MANUAL_REVIEW (待人工)
└── REJECT → SYSTEM_REJECT (拒绝)

MANUAL_REVIEW
    ↓
├── 人工通过 → FINAL_PASS
└── 人工拒绝 → FINAL_REJECT
```

---

## 五、关键数据表

### 5.1 risk_assessment（风控评估记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| apply_id | VARCHAR | 申请编号（唯一） |
| user_id | BIGINT | 用户ID |
| id_card | VARCHAR | 身份证号 |
| name | VARCHAR | 用户姓名 |
| status | VARCHAR | 状态 |
| total_score | INT | 总评分 |
| credit_limit | DECIMAL | 授信额度 |
| expire_date | DATETIME | 额度有效期 |
| is_final | BOOLEAN | 是否最终结果 |
| sys_decision | VARCHAR | 系统决策 |
| audit_remark | VARCHAR | 审核备注 |
| submit_time | DATETIME | 提交时间 |
| approval_time | DATETIME | 审批时间 |

### 5.2 user_external_features（用户外部特征）

| 字段 | 类型 | 说明 |
|------|------|------|
| sk_id_curr | BIGINT | 用户关联ID |
| id_card | VARCHAR | 身份证号 |
| days_birth | INT | 出生日期天数 |
| days_employed | INT | 入职天数 |
| amt_income_total | DECIMAL | 后台记录收入 |
| credit_bureau_week | INT | 近1周征信查询 |
| credit_bureau_mon | INT | 近1月征信查询 |
| active_loans_count | INT | 活跃贷款数 |
| ext_source_2/3 | DECIMAL | 第三方评分 |
| target | INT | 历史标签（0=正常，1=逾期） |
| prev_refused_count | INT | 历史被拒次数 |

---

## 六、评分影响因素

| 特征类别 | 特征项 | 影响方向 |
|----------|--------|----------|
| 个人信息 | 年龄（22-45岁最优） | + |
| 个人信息 | 学历（本科以上） | + |
| 个人信息 | 婚姻状况（已婚） | + |
| 资产状况 | 有房 | + |
| 资产状况 | 有车 | + |
| 收入水平 | 月收入 | + |
| 信用记录 | 活跃贷款数 | - |
| 信用记录 | 征信查询次数 | - |
| 信用记录 | 历史逾期 | - |
| 信用记录 | 历史被拒次数 | - |
