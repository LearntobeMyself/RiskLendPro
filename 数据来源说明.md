## 3.3 训练数据来源

### 3.3.1 数据源总览

本项目使用以下数据源支撑风控评分模型训练：

| 数据类型 | 文件路径 | 用途 |
|----------|----------|------|
| Home Credit 训练宽表 | `risk-assessment/data/raw/home_credit_train_min.parquet` | A 卡 / B 卡 LR 训练 |
| 失信被执行人黑名单 | `risk-assessment/data/raw/blacklist.csv` | 黑名单模型训练 |

### 3.3.2 Home Credit 训练宽表

#### 3.3.2.1 数据来源

(1) **原始来源**：Kaggle Home Credit Default Risk 竞赛数据集（2018年）

(2) **本地存储**：`risk-assessment/data/raw/home_credit_train_min.parquet`（社区精简版，约 108MB）

(3) **数据规模**：约 318 列特征，每行为一笔贷款申请

#### 3.3.2.2 A 卡入模特征

① **第三方征信特征**（4维）：

| 特征名 | 说明 |
|--------|------|
| `ext_source_3` | 外部权威综合分，IV 值最高 |
| `ext_source_2` | 外部权威综合分 |
| `phone_change_days` | 手机换号稳定性 |
| `prev_refused_count` | 历史被拒次数 |

② **申请表特征**（3维）：

| 特征名 | 说明 |
|--------|------|
| `edu_mid` | 学历档位（本科为1，其他为0） |
| `age_years` | 年龄（周岁） |
| `gender_male` | 性别（男为1，女为0） |

③ **标签字段**：`TARGET`（1 = 还款困难，0 = 正常）

#### 3.3.2.3 B 卡入模特征

B 卡使用贷后行为特征（7维）：

`inst_amt_paid_1y`、`inst_dbd_mean_total`、`inst_dbd_mean_1y`、`pos_dpd_mean`、`inst_late_ratio_total`、`inst_partial_payment_ratio`、`pos_dpd_def_mean`

### 3.3.3 黑名单数据

#### 3.3.3.1 数据来源

(1) **原始来源**：浙江温州市失信被执行人公开数据（约 2497 条）

(2) **本地存储**：`risk-assessment/data/raw/blacklist.csv`

(3) **更新方式**：支持全量导入和增量同步

#### 3.3.3.2 关键字段

| 字段名 | 说明 |
|--------|------|
| `name` | 被执行人姓名，支持通配符匹配 |
| `area_code` | 行政区划代码（身份证前6位） |
| `birth_year` | 出生年份 |
| `duty_status` | 履行情况（全部未履行/部分未履行） |
| `behavior_details` | 失信行为详情 |

#### 3.3.3.3 风险等级规则

(1) `全部未履行` → `HIGH`（高风险）

(2) `部分未履行` → `MEDIUM`（中风险）

(3) 其他 → `LOW`（低风险）

### 3.3.4 数据预处理流程

#### 3.3.4.1 A 卡数据处理

① **特征工程**：`feature_engineering_hc.py` 构建 WOE 候选特征

② **IV 筛选**：选取 IV ≥ 0.02 的特征入模

③ **WOE 分箱**：`woe_binning.py` 拟合分箱定义

④ **规则输出**：生成 `output/scoring_rules.json`

#### 3.3.4.2 B 卡数据处理

① **行为特征提取**：`clean_behavior_features.py` 提取 `inst_*`/`pos_*` 字段

② **规则输出**：生成 `output/b_scoring_rules.json`

#### 3.3.4.3 黑名单数据处理

① **数据清洗**：`clean_blacklist.py` 标准化字段格式

② **去重处理**：按 `(name, area_code, birth_year)` 去重

### 3.3.5 数据质量保障

#### 3.3.5.1 特征来源标注

(1) `application`：申请表特征

(2) `external`：第三方征信特征

(3) `derived`：派生特征

#### 3.3.5.2 数据验证

(1) 格式验证：身份证号、日期格式校验

(2) 范围验证：年龄、收入范围检查

(3) 一致性验证：收入偏差检测